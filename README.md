# Veritrust-AI
VeriTrust AI is a zero-trust compliance security proxy that protects enterprise data from leaking into public AI models. Operating locally with low latency, it intercepts employee prompts, strips out sensitive information (PII, credentials, API keys), and replaces them with context-aware synthetic tokens.
